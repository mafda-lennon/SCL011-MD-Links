const mdLinks = require('../');

describe('validPath', () => {

  it('should return true if is a file', () => {
    console.log()
  });

  it('should return true if extension is ".md"', () => {
    console.log()
  });
});

describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

});
